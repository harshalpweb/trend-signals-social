# Run skipped — 2026-08-28 routine, horizon 2026-08-29 (digest) / 2026-08-30 (build_in_public)

**Both posts in this run's 2-day horizon were skipped.** Not a per-post content
failure — a systemic Canva access blocker that would have hit every post in
this run identically, so retrying post-by-post would not have helped.

## Root cause

The `claude.ai Canva` connector attached to this session has **no access at
all** (not even read) to the locked master template `DAHSjFtuvnU`
("Premium Weekly Trend Report Cover") or its containing folder `FAHSjORWXy8`
(`TrendRadar`):

```
read-design(DAHSjFtuvnU, fields=[design_metadata]) ->
  {"warnings":[{"field":"design_metadata","reason":"Could not read design
    metadata: Not allowed to access design","code":"permission_denied"}]}

list-folder-items(FAHSjORWXy8) ->
  "Not allowed to access this folder"
```

`search-designs` from this connector does not surface `DAHSjFtuvnU` or any
folder-scoped TrendRadar design at all — only a handful of unrelated
one-off designs from account setup. This means the Canva account currently
behind this session's connector is not the same account (or does not have
the same sharing grant) that owns the locked master template.

Per `instagram-carousel/SKILL.md`, generating a fresh look-alike from
scratch is explicitly out of scope ("Don't regenerate from scratch each
week... AI generation from a text prompt is unreliable for exact brand
consistency") — this needs the actual locked template, not a substitute.

## What did complete before the block

- `instagram-signals` pulled and evaluated all 33 tracked entities against
  the mandatory family-aware freshness gate for 2026-08-29. Result: only 2
  entities passed —
  **saree** (composite 0.5667, conviction 1.0202, Amazon-bestseller data
  ~15h old) and **mixer grinder** (composite 0.5433, conviction 1.0079,
  Amazon-bestseller data ~15h old). Both single-source (`agreeing:
  ['marketplace']`).
  Four higher-raw-composite entities (gift hamper, face serum, rakhi, hair
  oil) failed the gate: their Google Trends search-family data hasn't
  refreshed since 2026-08-25 (~86h stale at run time), and three of the
  four have bit-identical composite scores across three consecutive daily
  snapshots (2026-08-26/27/28) — the exact frozen-source pattern the
  freeze-detection rule exists to catch.
- `instagram-caption` wrote and passed (`copydesk --caption`, 0 em-dashes,
  92 words) a digest caption built on that honest 2-mover leaderboard, with
  an explicit disclosure of the 4 held-out entities and why. Not saved to
  disk (no queue entry to attach it to without slides) — reproduced below
  so the work isn't lost.
- Sunday's build_in_public post wasn't started — no point drafting content
  for a post type that also needs the same blocked carousel step.

### Drafted digest caption (2026-08-29), ready to reuse once Canva access is fixed

> This week's top movers, by the numbers.
>
> 1. Saree. Composite score 0.5667, per Amazon bestseller data.
> 2. Mixer grinder. Composite score 0.5433, per Amazon bestseller data.
>
> Both readings are fresh, updated within the last day.
>
> Four other names on our radar (gift hamper, face serum, rakhi, hair oil) sat out this week. Their search data hasn't refreshed since Aug 25, so we're not ranking stale numbers as new ones. Worth watching once fresh data lands.
>
> Receipts only, even when the receipt is "we're waiting on better data."
>
> #TrendSpotting #DataDriven #AmazonBestsellers #IndiaShopping #MarketSignals

## What's needed to resume

Someone with founder-level access needs to either reconnect the
`claude.ai Canva` connector to the Canva account that owns `DAHSjFtuvnU` /
folder `FAHSjORWXy8`, or share that design + folder (edit access, not just
view) with whichever account the connector currently uses. This is a Canva
account/connector configuration action, not something resolvable by retrying
the same tool calls — verified via direct `read-design` and `list-folder-items`
calls in this run, both returning `permission_denied`.

Once fixed: re-run the routine (or just the `instagram-carousel` step for
these 2 posts using the signal/caption work already done above) to produce
and queue the Sat/Sun carousels.
